version: 1.0
deploy:
  force: bootstrap
  bootstrap:
    excludes:
      - var/bootstrap
    remote-commands:
      - echo "bootstrap"
  recurring:
    excludes:
      - var/recurring
    remote-commands:
      - echo "recurring"
